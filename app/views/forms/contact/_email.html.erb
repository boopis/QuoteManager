<div class="form-field">
  <%= f.hidden_field :type, value: 'email' %>

  <div class="form-group">
    <%= f.label :label, class: "sr-only" %>
    <div class="input-group">
      <span class="input-group-addon">
        <i class="fa fa-envelope"></i>
      </span>
      <%= f.text_field :label, class: "form-control", placeholder: "Email Field Label" %>
    </div>
    <div class="rr">
      <%= f.check_box :required %> <%= f.label 'required?' %>
      <%= link_to '#', class: 'remove_contact_fields', id: 'contact-email' do %>
        <%= content_tag(:i, "remove", class: "fa fa-minus-circle") %>
      <% end %>
    </div>
  </div>

</div>